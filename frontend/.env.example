# Leave this commented out and the app finds the backend automatically on
# whatever address the page was opened from — which is what makes the tablet
# work over wifi. Only set it if the backend lives on a different machine.
# VITE_API_URL=http://localhost:5000/api
VITE_CURRENCY=USD
